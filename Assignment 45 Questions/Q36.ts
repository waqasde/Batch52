function make_shirt(size: string, message: string): void {
    console.log(`The size of the shirt is ${size} and it says '${message}'.`);
}

// Example call to the function
make_shirt('Medium', 'Allah Save Palestine!');
